# Kaggle TPU v5e-8 validation, 2026-07-02

Source run:

- Log: `/kaggle/working/jax-scaling-hw-validation-v5e8-20260702-0422.log`
- Receipts: `/kaggle/working/jax-scaling-receipts-v5e8-20260702-0422.tgz`
- Commit under test: `5996ae4`
- Device: 8x `TPU v5 lite`

## Result summary

| Receipt | Prediction | Measurement | Verdict |
|---------|------------|-------------|---------|
| Roofline crossover batch | `[120, 480]` | `2048` | out of band |
| DP train step | `[2.13, 12.77] ms` | `7.69 ms` | in band |
| FSDP train step | `[2.13, 12.77] ms` | `8.56 ms` | in band |
| TP train step | `[2.13, 12.77] ms` | `29.59 ms` | out of band |
| Strategy loss spread | `[0.0, 0.005]` | `0.000004` | in band |
| Prefill/decode ratio, B=1 | `[2, 100000]` | `103.2` | in band |
| Decode throughput, B=1 | `[1835, 9177] tok/s` | `5800 tok/s` | in band |

FSDP trace over 20 steps:

- Compute: `98.4%`
- Communication: `1.4%`
- Memory: `0.3%`

TP trace over 20 steps:

- Compute: `86.7%`
- Communication: `13.1%`
- Memory: `0.2%`
- Top communication ops were all-reduces at about `15.4 ms` each.

## Notes

The small default run before this pass was dominated by fixed overhead. The
larger hardware pass fixed the training and inference bands for DP, FSDP, and
decode, which makes the remaining failures more specific.

Open questions:

- Roofline: the measured crossover moved farther out, from `1024` on the small
  sweep to `2048` on the extended sweep, while throughput was still climbing at
  `8192`. The current crossover detector is probably measuring "first point at
  half of observed peak", not the real asymptote.
- Tensor parallel training: TP compiled many more layer collectives and landed
  `3.5x` slower than FSDP. The trace confirms the cause: communication rises
  from `1.4%` in FSDP to `13.1%` in TP, with all-reduces dominating the
  communication-heavy top ops. The compute-only MFU band is missing this cost
  for the tiny transformer shape.
